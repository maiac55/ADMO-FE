import React,{useState} from 'react'; import {Pressable,StyleSheet,Text} from 'react-native'; import {NativeStackScreenProps} from '@react-navigation/native-stack'; import {AuthLayout} from '../components/AuthLayout'; import {AuthInput} from '../components/AuthInput'; import {PrimaryButton,SecondaryButton} from '../components/Buttons'; import {Divider} from '../components/Divider'; import {RootStackParamList} from '../navigation'; import {colors} from '../theme/colors'; export function LoginScreen({navigation}:NativeStackScreenProps<RootStackParamList,'Login'>){const[email,setEmail]=useState('');const[password,setPassword]=useState('');return <AuthLayout title="Welcome to ADMO" subtitle={'Your health, simplified.\nLog in to continue or create a new account.'}><AuthInput icon="mail-outline" placeholder="Email address" keyboardType="email-address" autoCapitalize="none" value={email} onChangeText={setEmail}/><AuthInput icon="lock-closed-outline" placeholder="Password" password value={password} onChangeText={setPassword}/><Pressable onPress={()=>navigation.navigate('ForgotPassword')}><Text style={s.link}>Forgot your password?</Text></Pressable><PrimaryButton label="Log in" onPress={()=>navigation.navigate('Home')} style={s.main}/><Divider/><SecondaryButton label="Create account" onPress={()=>navigation.navigate('Register')}/></AuthLayout>} const s=StyleSheet.create({link:{color:colors.teal,textDecorationLine:'underline',fontSize:12},main:{marginTop:28}});
